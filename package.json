{
    "name": "loyalty-b2b-mobility-root",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "build": "yarn workspaces run build",
        "dev": "yarn workspaces run dev"
    }
}